api:
  dashboard: true
  insecure: true

entryPoints:
  web:
    address: ":80"
  traefik:
    address: ":8080"

providers:
  file:
    filename: "/etc/traefik/dynamic_conf.yml"
    watch: true

accessLog:
  format: json

log:
  level: INFO
